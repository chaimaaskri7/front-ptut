
  # HTML code for interface

  This is a code bundle for HTML code for interface. The original project is available at https://www.figma.com/design/AaHmHehzKkpe8LgWv4EizN/HTML-code-for-interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  